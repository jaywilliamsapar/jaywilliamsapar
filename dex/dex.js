// String
const name = "John Doe";

// Number
const age = 25;

// Boolean
const isStudent = true;

// Array
const hobbies = ["reading", "painting", "playing guitar"];

// Object
const person = {
  name: "John Doe",
  age: 25,
  isStudent: true,
  hobbies: ["reading", "painting", "playing guitar"]
};

// Null
const myNullValue = null;

// Undefined
let myUndefinedValue;

// Date
const currentDate = new Date();

// Function
function greet(name) {
  console.log("Hello, " + name + "!");
}

// Output the values
console.log(name);
console.log(age);
console.log(isStudent);
console.log(hobbies);
console.log(person);
console.log(myNullValue);
console.log(myUndefinedValue);
console.log(currentDate);
greet("Alice");
